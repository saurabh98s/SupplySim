version: 2

sources:
  - name: raw
    tables:
      - name: supply_chain_data
  - name: live
    database: DEV
    schema: LIVE
    tables:
      - name: weekly_predictions
        description: "Weekly predictions generated by the ML model"
        columns:
          - name: DATE_INSERT
            tests:
              - not_null
          - name: SKU
            tests:
              - not_null
      - name: live_data
        description: "Live data collected from operations"
        columns:
          - name: DATE_INSERT
            tests:
              - not_null
          - name: SKU
            tests:
              - not_null

  - name: cleaned
    database: DEV
    schema: CLEANED
    tables:
      - name: supply_chain_data
        description: "Cleaned supply chain data"
